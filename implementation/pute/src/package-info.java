
/**
 * The task of this module is to compare two results of benchmarks by statistical method.
 * If the newer result is worse than the older one, then the module will report an error.
 */
package testComparator;
